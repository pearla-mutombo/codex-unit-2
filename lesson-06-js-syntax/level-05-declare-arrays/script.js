// Level 05 — Declare string and number arrays (same-line, plural names)
// TODO: Declare 5 plural-named string arrays and 5 plural-named number arrays, each with >=3 items.

// Example:
// let userNames = ["Ava", "Jon", "Maya"];

/* when i tested with 'const' it won't let me pass. 
only when i replace 'const' with 'let' would it let me "passed".*/
/* 5 Plural-named string arrays */
let userNames = [ "Tom", "Dick", "Harry"];
let favoriteCities = ["Tokyo", "Paris", "Dubai"];
let favoriteGenres = ["Sci-fi", "Comedy","Action"];
let fruitNames = ["Strawberry", "Green-Apple", "Mango"];
let codingLanaguages = ["JavaScript", "Syntax", "Boolean"];

/*5 Plural-named number arrays */
let luckyNumbers = [5, 18, 21];
let gameScores = [150, 350, 500];
let birthYears = [1984, 1995, 2001];
let prices = [9.99, 18.99, 39.99];
let zipCodes = [90120, 10001, 30305];

export default favoriteCities;
