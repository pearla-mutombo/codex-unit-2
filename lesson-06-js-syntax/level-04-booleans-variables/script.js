// Level 04 — Initialize boolean variables with question-word names
// TODO: Declare and initialize 10 boolean variables using names starting with is/has/can/should.

// Example:
// let isLoggedIn = false;

/* i can also use 'const' instead of 'let'.
 but it won't let me pass the test 
 if i use const. therefore i just used
let in lines 9 - 18 */
let isLoggedIn = false; 
let hasAccount = true;
let canEdit = false;
let shouldRefresh = true;
let isPaymentComplete = false;
let hasFreeShipping = true;
let canUploadFile = true;
let shouldChargeUser = false;
let isInDarkMode = true;
let hasWifiConnection = true;

export default hasWifiConnection;
