// Level 11 — Call provided function with args (10 calls)
// Helper provided for students; call it 10 times and save results to descriptive consts.

export function reportScore(playerName, score) {
  return playerName + " scored " + score + " points";
}

const avaScoreReportAva = reportScore("Ava", 50);

const avaScoreReportPreston = reportScore("Preston", 100);

const avaScoreReportZara = reportScore("Zara", 150);

const avaScoreReportNeo = reportScore("Neo", 200);

const avaScoreReportHarry = reportScore("Harry", 250);

const avaScoreReportRon = reportScore("Ron", 300);

const avaScoreReportHermonie = reportScore("Hermonie", 350);

const avaScoreReportMorphius = reportScore("Morphius", 400);

const avaScoreReportSmith = reportScore("Smith", 450);

const avaScoreReportOrecal = reportScore("Orecal", 500);

export default avaScoreReportOrecal;